[phases.setup]
nixPkgs = ['...', 'tesseract-ocr','libtesseract-dev']
[phases.install]
cmds = [ "python -m venv --copies /opt/venv && . /opt/venv/bin/activate && python -m pip install --upgrade pip && pip install -r requirements.txt"]

